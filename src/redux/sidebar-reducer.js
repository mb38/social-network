
let initialState = {
    friends: [
        {id: 1, name: 'Dimych'},
        {id: 2, name: 'Valera'},
        {id: 3, name: 'Sveta'},
        {id: 4, name: 'Grisha'},
    ],
}

const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;